[package]
name        = "flutter-embedder"
version     = "0.1.0"
edition     = "2021"
description = "OSCortex Flutter engine host — Phase 32-B userspace embedder binary"

# Standalone crate — not part of the workspace (different target, no_std userspace).
[workspace]

[[bin]]
name = "flutter-embedder"
path = "src/main.rs"

