[package]
name = "oscortex-hello"
version = "0.1.0"
edition = "2021"

# Keep out of root workspace — different build-std requirements.
[workspace]

[[bin]]
name = "hello"
path = "src/main.rs"

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
codegen-units = 1
strip = false
