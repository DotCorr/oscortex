diff --git a/engine/src/flutter/tools/gn b/engine/src/flutter/tools/gn
index 06a3e289..db5f59c4 100755
--- a/engine/src/flutter/tools/gn
+++ b/engine/src/flutter/tools/gn
@@ -92,6 +92,14 @@ def is_host_build(args):
   # If target_os == None, then this is a host build.
   if args.target_os is None:
     return True
+  # OSCortex native port: configure EXACTLY like a host (linux-x64) build.
+  # OSCortex runs linux-ABI binaries via its compatibility layer, so the host
+  # config is what actually links and runs; the is_oscortex flag (set below)
+  # just selects our platform backend sources. Going through the host path
+  # avoids the cross-target GN evaluation (e.g. ANGLE's wayland_dir scope) that a
+  # non-host target_os triggers. See docs/native-engine-port.md.
+  if args.target_os == 'oscortex':
+    return True
   # For linux arm64 builds, we cross compile from x64 hosts, so the
   # target_os='linux' and linux-cpu='arm64'
   if args.target_os == 'linux' and args.linux_cpu == 'arm64':
@@ -190,6 +198,8 @@ def get_target_cpu(args):
     return args.windows_cpu
   if args.target_os == 'qnx':
     return 'arm64'
+  if args.target_os == 'oscortex':
+    return 'x64'
 
   # Host build. Default to x64 unless overridden.
   if get_host_os() == 'mac' and args.mac_cpu:
@@ -526,6 +536,11 @@ def to_gn_args(args):
     gn_args['target_os'] = gn_args['host_os']
     gn_args['target_cpu'] = get_target_cpu(args)
     gn_args['dart_target_arch'] = gn_args['target_cpu']
+    # OSCortex native port: identical to the host build, plus the flag that
+    # selects our platform backend. The out/ dir is named from args.target_os
+    # ('oscortex'), so it does not collide with a plain host build.
+    if args.target_os == 'oscortex':
+      gn_args['is_oscortex'] = True
   else:
     gn_args['target_os'] = args.target_os
     gn_args['target_cpu'] = get_target_cpu(args)
@@ -652,6 +667,11 @@ def to_gn_args(args):
     gn_args['skia_enable_graphite'] = False
     gn_args['arm_use_neon'] = False
   else:
+    # OSCortex falls through here too: it builds the full host graphics config
+    # (skia_use_gl = True) and selects the SOFTWARE renderer at RUNTIME via the
+    # embedder's kSoftware config, so the GPU backends compiled here are never
+    # invoked. Build-time trimming needs a fully self-consistent flag set (a
+    # later optimization), so we keep the proven host config for correctness.
     gn_args['skia_use_gl'] = args.target_os != 'fuchsia'
 
   if sys.platform == 'darwin' and args.target_os not in ['android', 'fuchsia', 'linux']:
@@ -965,7 +985,10 @@ def parse_args(args):
   parser.add_argument(
       '--target-os',
       type=str,
-      choices=['android', 'ios', 'mac', 'linux', 'fuchsia', 'wasm', 'win', 'qnx']
+      choices=[
+          'android', 'ios', 'mac', 'linux', 'fuchsia', 'wasm', 'win', 'qnx',
+          'oscortex'
+      ]
   )
   parser.add_argument('--android', dest='target_os', action='store_const', const='android')
   parser.add_argument(
